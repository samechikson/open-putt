fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
# Firestore persistence (Native mode, reached via the Admin/service-account
# client) + Firebase Auth ID-token verification. firebase-admin pulls in
# google-cloud-firestore, but pin it explicitly since db.py imports it directly.
google-cloud-firestore>=2.16.0
firebase-admin>=6.5.0
