fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
opencv-contrib-python-headless>=4.9.0
numpy>=1.26.0
python-dotenv>=1.0.0
google-cloud-storage>=2.16.0
google-cloud-tasks>=2.16.0
# Cloud SQL (Postgres) persistence + Firebase Auth token verification.
# On Cloud Run we connect over the Unix socket mounted by
# --add-cloudsql-instances, so a plain psycopg conninfo is enough (no connector).
psycopg[binary,pool]>=3.2.0
firebase-admin>=6.5.0
